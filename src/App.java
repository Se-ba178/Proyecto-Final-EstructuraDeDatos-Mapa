public class App {

    public static void main(String[] args) {
        //Aqui utilizamos para hacer pruebas antes de realizar la interfaz 
        // Graph<String> mapa = new Graph<>();
        // mapa.add("A");
        // mapa.add("B");
        // mapa.add("C");
        // mapa.add("D");

        // mapa.addEdge("A", "B");
        // mapa.addEdge("A", "C");
        // mapa.addEdge("B", "D");
        // mapa.addEdge("C", "D");

        // mapa.printGrafo();

        // PathFinder<String> bfs = new BFSPathFinder<>();

        // PathResult<String> resultado = bfs.find(mapa, "A", "D");

        // System.out.println(resultado);



    }
}